# Names only: this file is not automatically loaded.
# Inject the appropriate reader or writer credentials into its process.
SUPABASE_URL=
SUPABASE_KEY=
# Server process only: short-lived JWT with role portfolio_writer.
# Reader processes omit this variable (or use an authenticated reader JWT).
# Use a publishable/anon API key above; do not use a service-role/secret key.
SUPABASE_ACCESS_TOKEN=
